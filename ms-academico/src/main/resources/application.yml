spring:
  application:
    name: ms-academico
  config:
    import: "configserver:http://localhost:8888"
  cloud:
    openfeign:
      client:
        config:
          default:
            connect-timeout: 3000
            read-timeout: 5000